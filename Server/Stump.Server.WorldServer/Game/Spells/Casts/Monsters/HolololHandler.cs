using Stump.DofusProtocol.Enums;
using Stump.Server.WorldServer.Game.Fights;

namespace Stump.Server.WorldServer.Game.Spells.Casts.Ecaflip
{
    [SpellCastHandler(SpellIdEnum.HOLOLOL_1039)] //Debug até que a mecânica esteja correta.
    public class HolololHandler : DefaultSpellCastHandler
    {
        public HolololHandler(SpellCastInformations cast) : base(cast)
        { }

        public override void Execute()
        {
            if (!base.Initialize())
                Initialize();

            //Handlers[0].Apply();
            //Handlers[1].Apply();
            //Handlers[2].Apply();
            //Handlers[3].Apply();
            //Handlers[4].Apply();
            //Handlers[5].Apply();
            //Handlers[6].Apply();
            //Handlers[7].Apply();
            //Handlers[8].Apply();
            //Handlers[9].Apply();
            //Handlers[10].Apply();
            //Handlers[11].Apply();
            //Handlers[12].Apply();
            //Handlers[13].Apply();
            //Handlers[14].Apply();
            //Handlers[15].Apply();
            //Handlers[16].Apply();
            //Handlers[17].Apply();
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class InteractiveElement
    {
        public const short Id = 6006;
        public virtual short TypeId
        {
            get { return Id; }
        }
        
        public int elementId;
        public int elementTypeId;
        public IEnumerable<Types.InteractiveElementSkill> enabledSkills;
        public IEnumerable<Types.InteractiveElementSkill> disabledSkills;
        public bool onCurrentMap;
        
        public InteractiveElement()
        {
        }
        
        public InteractiveElement(int elementId, int elementTypeId, IEnumerable<Types.InteractiveElementSkill> enabledSkills, IEnumerable<Types.InteractiveElementSkill> disabledSkills, bool onCurrentMap)
        {
            this.elementId = elementId;
            this.elementTypeId = elementTypeId;
            this.enabledSkills = enabledSkills;
            this.disabledSkills = disabledSkills;
            this.onCurrentMap = onCurrentMap;
        }
        
        public virtual void Serialize(IDataWriter writer)
        {
            writer.WriteInt(elementId);
            writer.WriteInt(elementTypeId);
            writer.WriteShort((short)enabledSkills.Count());
            foreach (var objectToSend in enabledSkills)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)disabledSkills.Count());
            foreach (var objectToSend in disabledSkills)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
            writer.WriteBoolean(onCurrentMap);
        }
        
        public virtual void Deserialize(IDataReader reader)
        {
            elementId = reader.ReadInt();
            elementTypeId = reader.ReadInt();
            var enabledSkillsCount = reader.ReadUShort();
            var enabledSkills_ = new Types.InteractiveElementSkill[enabledSkillsCount];
            for (var enabledSkillsIndex = 0; enabledSkillsIndex < enabledSkillsCount; enabledSkillsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.InteractiveElementSkill>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 enabledSkills_[enabledSkillsIndex] = objectToAdd;
            }
            enabledSkills = enabledSkills_;
            var disabledSkillsCount = reader.ReadUShort();
            var disabledSkills_ = new Types.InteractiveElementSkill[disabledSkillsCount];
            for (var disabledSkillsIndex = 0; disabledSkillsIndex < disabledSkillsCount; disabledSkillsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.InteractiveElementSkill>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 disabledSkills_[disabledSkillsIndex] = objectToAdd;
            }
            disabledSkills = disabledSkills_;
            onCurrentMap = reader.ReadBoolean();
        }
    }
}
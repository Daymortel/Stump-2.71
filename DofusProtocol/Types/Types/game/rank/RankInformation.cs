

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class RankInformation : RankMinimalInformation
    {
        public const short Id = 4716;
        public override short TypeId
        {
            get { return Id; }
        }
        
        public uint order;
        public uint gfxId;
        public bool modifiable;
        public IEnumerable<uint> rights;
        
        public RankInformation()
        {
        }
        
        public RankInformation(uint id, string name, uint order, uint gfxId, bool modifiable, IEnumerable<uint> rights)
         : base(id, name)
        {
            this.order = order;
            this.gfxId = gfxId;
            this.modifiable = modifiable;
            this.rights = rights;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteVarUInt(order);
            writer.WriteVarUInt(gfxId);
            writer.WriteBoolean(modifiable);
            writer.WriteShort((short)rights.Count());
            foreach (var objectToSend in rights)
            {
                 writer.WriteVarUInt(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            order = reader.ReadVarUInt();
            gfxId = reader.ReadVarUInt();
            modifiable = reader.ReadBoolean();
            var rightsCount = reader.ReadUShort();
            var rights_ = new uint[rightsCount];
            for (var rightsIndex = 0; rightsIndex < rightsCount; rightsIndex++)
            {
                 rights_[rightsIndex] = reader.ReadVarUInt();
            }
            rights = rights_;
        }
    }
}
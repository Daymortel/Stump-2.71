

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class AlterationInfo
    {
        public const short Id = 7621;
        public virtual short TypeId
        {
            get { return Id; }
        }
        
        public uint alterationId;
        public double creationTime;
        public sbyte expirationType;
        public double expirationValue;
        public IEnumerable<Types.ObjectEffect> effects;
        
        public AlterationInfo()
        {
        }
        
        public AlterationInfo(uint alterationId, double creationTime, sbyte expirationType, double expirationValue, IEnumerable<Types.ObjectEffect> effects)
        {
            this.alterationId = alterationId;
            this.creationTime = creationTime;
            this.expirationType = expirationType;
            this.expirationValue = expirationValue;
            this.effects = effects;
        }
        
        public virtual void Serialize(IDataWriter writer)
        {
            writer.WriteUInt(alterationId);
            writer.WriteDouble(creationTime);
            writer.WriteSByte(expirationType);
            writer.WriteDouble(expirationValue);
            writer.WriteShort((short)effects.Count());
            foreach (var objectToSend in effects)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public virtual void Deserialize(IDataReader reader)
        {
            alterationId = reader.ReadUInt();
            creationTime = reader.ReadDouble();
            expirationType = reader.ReadSByte();
            expirationValue = reader.ReadDouble();
            var effectsCount = reader.ReadUShort();
            var effects_ = new Types.ObjectEffect[effectsCount];
            for (var effectsIndex = 0; effectsIndex < effectsCount; effectsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.ObjectEffect>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 effects_[effectsIndex] = objectToAdd;
            }
            effects = effects_;
        }
    }
}
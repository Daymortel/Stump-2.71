

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class FightExternalInformations
    {
        public const short Id = 5040;
        public virtual short TypeId
        {
            get { return Id; }
        }
        
        public ushort fightId;
        public sbyte fightType;
        public int fightStart;
        public bool fightSpectatorLocked;
        public IEnumerable<Types.FightTeamLightInformations> fightTeams;
        public IEnumerable<Types.FightOptionsInformations> fightTeamsOptions;
        
        public FightExternalInformations()
        {
        }
        
        public FightExternalInformations(ushort fightId, sbyte fightType, int fightStart, bool fightSpectatorLocked, IEnumerable<Types.FightTeamLightInformations> fightTeams, IEnumerable<Types.FightOptionsInformations> fightTeamsOptions)
        {
            this.fightId = fightId;
            this.fightType = fightType;
            this.fightStart = fightStart;
            this.fightSpectatorLocked = fightSpectatorLocked;
            this.fightTeams = fightTeams;
            this.fightTeamsOptions = fightTeamsOptions;
        }
        
        public virtual void Serialize(IDataWriter writer)
        {
            writer.WriteVarUShort(fightId);
            writer.WriteSByte(fightType);
            writer.WriteInt(fightStart);
            writer.WriteBoolean(fightSpectatorLocked);
            foreach (var objectToSend in fightTeams)
            {
                 objectToSend.Serialize(writer);
            }
            foreach (var objectToSend in fightTeamsOptions)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public virtual void Deserialize(IDataReader reader)
        {
            fightId = reader.ReadVarUShort();
            fightType = reader.ReadSByte();
            fightStart = reader.ReadInt();
            fightSpectatorLocked = reader.ReadBoolean();
            var fightTeams_ = new Types.FightTeamLightInformations[2];
            for (var fightTeamsIndex = 0; fightTeamsIndex < 2; fightTeamsIndex++)
            {
                 var objectToAdd = new Types.FightTeamLightInformations();
                 objectToAdd.Deserialize(reader);
                 fightTeams_[fightTeamsIndex] = objectToAdd;
            }
            fightTeams = fightTeams_;
            var fightTeamsOptions_ = new Types.FightOptionsInformations[2];
            for (var fightTeamsOptionsIndex = 0; fightTeamsOptionsIndex < 2; fightTeamsOptionsIndex++)
            {
                 var objectToAdd = new Types.FightOptionsInformations();
                 objectToAdd.Deserialize(reader);
                 fightTeamsOptions_[fightTeamsOptionsIndex] = objectToAdd;
            }
            fightTeamsOptions = fightTeamsOptions_;
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class ChallengeTargetWithAttackerInformation : ChallengeTargetInformation
    {
        public const short Id = 8417;
        public override short TypeId
        {
            get { return Id; }
        }
        
        public IEnumerable<double> attackersIds;
        
        public ChallengeTargetWithAttackerInformation()
        {
        }
        
        public ChallengeTargetWithAttackerInformation(double targetId, short targetCell, IEnumerable<double> attackersIds)
         : base(targetId, targetCell)
        {
            this.attackersIds = attackersIds;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteShort((short)attackersIds.Count());
            foreach (var objectToSend in attackersIds)
            {
                 writer.WriteDouble(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            var attackersIdsCount = reader.ReadUShort();
            var attackersIds_ = new double[attackersIdsCount];
            for (var attackersIdsIndex = 0; attackersIdsIndex < attackersIdsCount; attackersIdsIndex++)
            {
                 attackersIds_[attackersIdsIndex] = reader.ReadDouble();
            }
            attackersIds = attackersIds_;
        }
    }
}
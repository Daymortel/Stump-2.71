

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class AtlasPointsInformations
    {
        public const short Id = 9930;
        public virtual short TypeId
        {
            get { return Id; }
        }
        
        public sbyte type;
        public IEnumerable<Types.MapCoordinatesExtended> coords;
        
        public AtlasPointsInformations()
        {
        }
        
        public AtlasPointsInformations(sbyte type, IEnumerable<Types.MapCoordinatesExtended> coords)
        {
            this.type = type;
            this.coords = coords;
        }
        
        public virtual void Serialize(IDataWriter writer)
        {
            writer.WriteSByte(type);
            writer.WriteShort((short)coords.Count());
            foreach (var objectToSend in coords)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public virtual void Deserialize(IDataReader reader)
        {
            type = reader.ReadSByte();
            var coordsCount = reader.ReadUShort();
            var coords_ = new Types.MapCoordinatesExtended[coordsCount];
            for (var coordsIndex = 0; coordsIndex < coordsCount; coordsIndex++)
            {
                 var objectToAdd = new Types.MapCoordinatesExtended();
                 objectToAdd.Deserialize(reader);
                 coords_[coordsIndex] = objectToAdd;
            }
            coords = coords_;
        }
    }
}
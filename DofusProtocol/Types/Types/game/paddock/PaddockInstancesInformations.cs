

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class PaddockInstancesInformations : PaddockInformations
    {
        public const short Id = 6034;
        public override short TypeId
        {
            get { return Id; }
        }
        
        public IEnumerable<Types.PaddockBuyableInformations> paddocks;
        
        public PaddockInstancesInformations()
        {
        }
        
        public PaddockInstancesInformations(ushort maxOutdoorMount, ushort maxItems, IEnumerable<Types.PaddockBuyableInformations> paddocks)
         : base(maxOutdoorMount, maxItems)
        {
            this.paddocks = paddocks;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteShort((short)paddocks.Count());
            foreach (var objectToSend in paddocks)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            var paddocksCount = reader.ReadUShort();
            var paddocks_ = new Types.PaddockBuyableInformations[paddocksCount];
            for (var paddocksIndex = 0; paddocksIndex < paddocksCount; paddocksIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.PaddockBuyableInformations>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 paddocks_[paddocksIndex] = objectToAdd;
            }
            paddocks = paddocks_;
        }
    }
}
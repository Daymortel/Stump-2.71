

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
    public class KohAllianceInfo
    {
        public const short Id = 5284;
        public virtual short TypeId
        {
            get { return Id; }
        }
        
        public Types.AllianceInformation alliance;
        public ulong memberCount;
        public IEnumerable<Types.KohAllianceRoleMembers> kohAllianceRoleMembers;
        public IEnumerable<Types.KohScore> scores;
        public uint matchDominationScores;
        
        public KohAllianceInfo()
        {
        }
        
        public KohAllianceInfo(Types.AllianceInformation alliance, ulong memberCount, IEnumerable<Types.KohAllianceRoleMembers> kohAllianceRoleMembers, IEnumerable<Types.KohScore> scores, uint matchDominationScores)
        {
            this.alliance = alliance;
            this.memberCount = memberCount;
            this.kohAllianceRoleMembers = kohAllianceRoleMembers;
            this.scores = scores;
            this.matchDominationScores = matchDominationScores;
        }
        
        public virtual void Serialize(IDataWriter writer)
        {
            alliance.Serialize(writer);
            writer.WriteVarULong(memberCount);
            writer.WriteShort((short)kohAllianceRoleMembers.Count());
            foreach (var objectToSend in kohAllianceRoleMembers)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)scores.Count());
            foreach (var objectToSend in scores)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteVarUInt(matchDominationScores);
        }
        
        public virtual void Deserialize(IDataReader reader)
        {
            alliance = new Types.AllianceInformation();
            alliance.Deserialize(reader);
            memberCount = reader.ReadVarULong();
            var kohAllianceRoleMembersCount = reader.ReadUShort();
            var kohAllianceRoleMembers_ = new Types.KohAllianceRoleMembers[kohAllianceRoleMembersCount];
            for (var kohAllianceRoleMembersIndex = 0; kohAllianceRoleMembersIndex < kohAllianceRoleMembersCount; kohAllianceRoleMembersIndex++)
            {
                 var objectToAdd = new Types.KohAllianceRoleMembers();
                 objectToAdd.Deserialize(reader);
                 kohAllianceRoleMembers_[kohAllianceRoleMembersIndex] = objectToAdd;
            }
            kohAllianceRoleMembers = kohAllianceRoleMembers_;
            var scoresCount = reader.ReadUShort();
            var scores_ = new Types.KohScore[scoresCount];
            for (var scoresIndex = 0; scoresIndex < scoresCount; scoresIndex++)
            {
                 var objectToAdd = new Types.KohScore();
                 objectToAdd.Deserialize(reader);
                 scores_[scoresIndex] = objectToAdd;
            }
            scores = scores_;
            matchDominationScores = reader.ReadVarUInt();
        }
    }
}
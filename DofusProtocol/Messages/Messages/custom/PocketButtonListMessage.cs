

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class PocketButtonListMessage : Message
    {
        public const uint Id = 9993;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.PocketButton> buttons;
        
        public PocketButtonListMessage()
        { }
        
        public PocketButtonListMessage(IEnumerable<Types.PocketButton> buttons)
        {
            this.buttons = buttons;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)buttons.Count());
            foreach (var objectToSend in buttons)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var buttonsCount = reader.ReadUShort();
            var buttons_ = new Types.PocketButton[buttonsCount];
            for (var buttonsIndex = 0; buttonsIndex < buttonsCount; buttonsIndex++)
            {
                 var objectToAdd = new Types.PocketButton();
                 objectToAdd.Deserialize(reader);
                 buttons_[buttonsIndex] = objectToAdd;
            }
            buttons = buttons_;
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class PocketTeleporterDestinationsMessage : Message
    {
        public const uint Id = 3;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.PocketTeleporterDestination> destinations;
        
        public PocketTeleporterDestinationsMessage()
        { }
        
        public PocketTeleporterDestinationsMessage(IEnumerable<Types.PocketTeleporterDestination> destinations)
        {
            this.destinations = destinations;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)destinations.Count());
            foreach (var objectToSend in destinations)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var destinationsCount = reader.ReadUShort();
            var destinations_ = new Types.PocketTeleporterDestination[destinationsCount];
            for (var destinationsIndex = 0; destinationsIndex < destinationsCount; destinationsIndex++)
            {
                 var objectToAdd = new Types.PocketTeleporterDestination();
                 objectToAdd.Deserialize(reader);
                 destinations_[destinationsIndex] = objectToAdd;
            }
            destinations = destinations_;
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class AuraEmoteListMessage : Message
    {
        public const uint Id = 9990;
        public override uint MessageId => Id;
        
        public uint currentAuraId;
        public IEnumerable<Types.AuraEmoteItem> unlockedAuras;
        public IEnumerable<Types.AuraEmoteItem> allAuras;
        
        public AuraEmoteListMessage()
        { }
        
        public AuraEmoteListMessage(uint currentAuraId, IEnumerable<Types.AuraEmoteItem> unlockedAuras, IEnumerable<Types.AuraEmoteItem> allAuras)
        {
            this.currentAuraId = currentAuraId;
            this.unlockedAuras = unlockedAuras;
            this.allAuras = allAuras;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteUInt(currentAuraId);
            writer.WriteShort((short)unlockedAuras.Count());
            foreach (var objectToSend in unlockedAuras)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)allAuras.Count());
            foreach (var objectToSend in allAuras)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            currentAuraId = reader.ReadUInt();
            var unlockedAurasCount = reader.ReadUShort();
            var unlockedAuras_ = new Types.AuraEmoteItem[unlockedAurasCount];
            for (var unlockedAurasIndex = 0; unlockedAurasIndex < unlockedAurasCount; unlockedAurasIndex++)
            {
                 var objectToAdd = new Types.AuraEmoteItem();
                 objectToAdd.Deserialize(reader);
                 unlockedAuras_[unlockedAurasIndex] = objectToAdd;
            }
            unlockedAuras = unlockedAuras_;
            var allAurasCount = reader.ReadUShort();
            var allAuras_ = new Types.AuraEmoteItem[allAurasCount];
            for (var allAurasIndex = 0; allAurasIndex < allAurasCount; allAurasIndex++)
            {
                 var objectToAdd = new Types.AuraEmoteItem();
                 objectToAdd.Deserialize(reader);
                 allAuras_[allAurasIndex] = objectToAdd;
            }
            allAuras = allAuras_;
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ServersListMessage : Message
    {
        public const uint Id = 6921;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GameServerInformations> servers;
        public bool canCreateNewCharacter;
        
        public ServersListMessage()
        { }
        
        public ServersListMessage(IEnumerable<Types.GameServerInformations> servers, bool canCreateNewCharacter)
        {
            this.servers = servers;
            this.canCreateNewCharacter = canCreateNewCharacter;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)servers.Count());
            foreach (var objectToSend in servers)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteBoolean(canCreateNewCharacter);
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var serversCount = reader.ReadUShort();
            var servers_ = new Types.GameServerInformations[serversCount];
            for (var serversIndex = 0; serversIndex < serversCount; serversIndex++)
            {
                 var objectToAdd = new Types.GameServerInformations();
                 objectToAdd.Deserialize(reader);
                 servers_[serversIndex] = objectToAdd;
            }
            servers = servers_;
            canCreateNewCharacter = reader.ReadBoolean();
        }
    }
}
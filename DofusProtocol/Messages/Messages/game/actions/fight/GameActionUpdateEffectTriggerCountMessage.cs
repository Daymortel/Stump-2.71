

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameActionUpdateEffectTriggerCountMessage : Message
    {
        public const uint Id = 6965;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GameFightEffectTriggerCount> targetIds;
        
        public GameActionUpdateEffectTriggerCountMessage()
        { }
        
        public GameActionUpdateEffectTriggerCountMessage(IEnumerable<Types.GameFightEffectTriggerCount> targetIds)
        {
            this.targetIds = targetIds;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)targetIds.Count());
            foreach (var objectToSend in targetIds)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var targetIdsCount = reader.ReadUShort();
            var targetIds_ = new Types.GameFightEffectTriggerCount[targetIdsCount];
            for (var targetIdsIndex = 0; targetIdsIndex < targetIdsCount; targetIdsIndex++)
            {
                 var objectToAdd = new Types.GameFightEffectTriggerCount();
                 objectToAdd.Deserialize(reader);
                 targetIds_[targetIdsIndex] = objectToAdd;
            }
            targetIds = targetIds_;
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameActionFightMultipleSummonMessage : AbstractGameActionMessage
    {
        public const uint Id = 2358;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GameContextSummonsInformation> summons;
        
        public GameActionFightMultipleSummonMessage()
        { }
        
        public GameActionFightMultipleSummonMessage(ushort actionId, double sourceId, IEnumerable<Types.GameContextSummonsInformation> summons)
         : base(actionId, sourceId)
        {
            this.summons = summons;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteShort((short)summons.Count());
            foreach (var objectToSend in summons)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            var summonsCount = reader.ReadUShort();
            var summons_ = new Types.GameContextSummonsInformation[summonsCount];
            for (var summonsIndex = 0; summonsIndex < summonsCount; summonsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.GameContextSummonsInformation>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 summons_[summonsIndex] = objectToAdd;
            }
            summons = summons_;
        }
    }
}
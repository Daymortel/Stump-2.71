

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class PrismsListMessage : Message
    {
        public const uint Id = 2243;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.PrismGeolocalizedInformation> prisms;
        
        public PrismsListMessage()
        { }
        
        public PrismsListMessage(IEnumerable<Types.PrismGeolocalizedInformation> prisms)
        {
            this.prisms = prisms;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)prisms.Count());
            foreach (var objectToSend in prisms)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var prismsCount = reader.ReadUShort();
            var prisms_ = new Types.PrismGeolocalizedInformation[prismsCount];
            for (var prismsIndex = 0; prismsIndex < prismsCount; prismsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.PrismGeolocalizedInformation>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 prisms_[prismsIndex] = objectToAdd;
            }
            prisms = prisms_;
        }
    }
}
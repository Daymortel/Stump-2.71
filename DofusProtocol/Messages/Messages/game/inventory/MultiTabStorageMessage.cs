

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class MultiTabStorageMessage : Message
    {
        public const uint Id = 3530;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.StorageTabInformation> tabs;
        
        public MultiTabStorageMessage()
        { }
        
        public MultiTabStorageMessage(IEnumerable<Types.StorageTabInformation> tabs)
        {
            this.tabs = tabs;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)tabs.Count());
            foreach (var objectToSend in tabs)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var tabsCount = reader.ReadUShort();
            var tabs_ = new Types.StorageTabInformation[tabsCount];
            for (var tabsIndex = 0; tabsIndex < tabsCount; tabsIndex++)
            {
                 var objectToAdd = new Types.StorageTabInformation();
                 objectToAdd.Deserialize(reader);
                 tabs_[tabsIndex] = objectToAdd;
            }
            tabs = tabs_;
        }
    }
}
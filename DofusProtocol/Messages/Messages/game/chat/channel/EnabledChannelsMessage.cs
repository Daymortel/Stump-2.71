

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class EnabledChannelsMessage : Message
    {
        public const uint Id = 8311;
        public override uint MessageId => Id;
        
        public IEnumerable<sbyte> channels;
        public IEnumerable<sbyte> disallowed;
        
        public EnabledChannelsMessage()
        { }
        
        public EnabledChannelsMessage(IEnumerable<sbyte> channels, IEnumerable<sbyte> disallowed)
        {
            this.channels = channels;
            this.disallowed = disallowed;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)channels.Count());
            foreach (var objectToSend in channels)
            {
                 writer.WriteSByte(objectToSend);
            }
            writer.WriteShort((short)disallowed.Count());
            foreach (var objectToSend in disallowed)
            {
                 writer.WriteSByte(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var channelsCount = reader.ReadUShort();
            var channels_ = new sbyte[channelsCount];
            for (var channelsIndex = 0; channelsIndex < channelsCount; channelsIndex++)
            {
                 channels_[channelsIndex] = reader.ReadSByte();
            }
            channels = channels_;
            var disallowedCount = reader.ReadUShort();
            var disallowed_ = new sbyte[disallowedCount];
            for (var disallowedIndex = 0; disallowedIndex < disallowedCount; disallowedIndex++)
            {
                 disallowed_[disallowedIndex] = reader.ReadSByte();
            }
            disallowed = disallowed_;
        }
    }
}
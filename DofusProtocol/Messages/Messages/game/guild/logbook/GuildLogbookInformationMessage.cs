

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GuildLogbookInformationMessage : Message
    {
        public const uint Id = 8089;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GuildLogbookEntryBasicInformation> globalActivities;
        public IEnumerable<Types.GuildLogbookEntryBasicInformation> chestActivities;
        
        public GuildLogbookInformationMessage()
        { }
        
        public GuildLogbookInformationMessage(IEnumerable<Types.GuildLogbookEntryBasicInformation> globalActivities, IEnumerable<Types.GuildLogbookEntryBasicInformation> chestActivities)
        {
            this.globalActivities = globalActivities;
            this.chestActivities = chestActivities;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)globalActivities.Count());
            foreach (var objectToSend in globalActivities)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)chestActivities.Count());
            foreach (var objectToSend in chestActivities)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var globalActivitiesCount = reader.ReadUShort();
            var globalActivities_ = new Types.GuildLogbookEntryBasicInformation[globalActivitiesCount];
            for (var globalActivitiesIndex = 0; globalActivitiesIndex < globalActivitiesCount; globalActivitiesIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.GuildLogbookEntryBasicInformation>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 globalActivities_[globalActivitiesIndex] = objectToAdd;
            }
            globalActivities = globalActivities_;
            var chestActivitiesCount = reader.ReadUShort();
            var chestActivities_ = new Types.GuildLogbookEntryBasicInformation[chestActivitiesCount];
            for (var chestActivitiesIndex = 0; chestActivitiesIndex < chestActivitiesCount; chestActivitiesIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.GuildLogbookEntryBasicInformation>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 chestActivities_[chestActivitiesIndex] = objectToAdd;
            }
            chestActivities = chestActivities_;
        }
    }
}
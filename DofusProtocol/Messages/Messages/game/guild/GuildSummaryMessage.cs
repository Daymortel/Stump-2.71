

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GuildSummaryMessage : PaginationAnswerAbstractMessage
    {
        public const uint Id = 6955;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GuildFactSheetInformations> guilds;
        
        public GuildSummaryMessage()
        { }
        
        public GuildSummaryMessage(double offset, uint count, uint total, IEnumerable<Types.GuildFactSheetInformations> guilds)
         : base(offset, count, total)
        {
            this.guilds = guilds;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteShort((short)guilds.Count());
            foreach (var objectToSend in guilds)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            var guildsCount = reader.ReadUShort();
            var guilds_ = new Types.GuildFactSheetInformations[guildsCount];
            for (var guildsIndex = 0; guildsIndex < guildsCount; guildsIndex++)
            {
                 var objectToAdd = new Types.GuildFactSheetInformations();
                 objectToAdd.Deserialize(reader);
                 guilds_[guildsIndex] = objectToAdd;
            }
            guilds = guilds_;
        }
    }
}
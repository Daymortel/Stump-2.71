

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GuildRanksMessage : Message
    {
        public const uint Id = 433;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.RankInformation> ranks;
        
        public GuildRanksMessage()
        { }
        
        public GuildRanksMessage(IEnumerable<Types.RankInformation> ranks)
        {
            this.ranks = ranks;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)ranks.Count());
            foreach (var objectToSend in ranks)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var ranksCount = reader.ReadUShort();
            var ranks_ = new Types.RankInformation[ranksCount];
            for (var ranksIndex = 0; ranksIndex < ranksCount; ranksIndex++)
            {
                 var objectToAdd = new Types.RankInformation();
                 objectToAdd.Deserialize(reader);
                 ranks_[ranksIndex] = objectToAdd;
            }
            ranks = ranks_;
        }
    }
}
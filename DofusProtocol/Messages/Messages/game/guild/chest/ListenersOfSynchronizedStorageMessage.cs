

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ListenersOfSynchronizedStorageMessage : Message
    {
        public const uint Id = 2330;
        public override uint MessageId => Id;
        
        public IEnumerable<string> players;
        
        public ListenersOfSynchronizedStorageMessage()
        { }
        
        public ListenersOfSynchronizedStorageMessage(IEnumerable<string> players)
        {
            this.players = players;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)players.Count());
            foreach (var objectToSend in players)
            {
                 writer.WriteUTF(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var playersCount = reader.ReadUShort();
            var players_ = new string[playersCount];
            for (var playersIndex = 0; playersIndex < playersCount; playersIndex++)
            {
                 players_[playersIndex] = reader.ReadUTF();
            }
            players = players_;
        }
    }
}
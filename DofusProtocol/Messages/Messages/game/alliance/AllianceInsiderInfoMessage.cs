

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class AllianceInsiderInfoMessage : Message
    {
        public const uint Id = 3419;
        public override uint MessageId => Id;
        
        public Types.AllianceFactSheetInformation allianceInfos;
        public IEnumerable<Types.AllianceMemberInfo> members;
        public IEnumerable<Types.PrismGeolocalizedInformation> prisms;
        public IEnumerable<Types.TaxCollectorInformations> taxCollectors;
        
        public AllianceInsiderInfoMessage()
        { }
        
        public AllianceInsiderInfoMessage(Types.AllianceFactSheetInformation allianceInfos, IEnumerable<Types.AllianceMemberInfo> members, IEnumerable<Types.PrismGeolocalizedInformation> prisms, IEnumerable<Types.TaxCollectorInformations> taxCollectors)
        {
            this.allianceInfos = allianceInfos;
            this.members = members;
            this.prisms = prisms;
            this.taxCollectors = taxCollectors;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            allianceInfos.Serialize(writer);
            writer.WriteShort((short)members.Count());
            foreach (var objectToSend in members)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)prisms.Count());
            foreach (var objectToSend in prisms)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)taxCollectors.Count());
            foreach (var objectToSend in taxCollectors)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            allianceInfos = new Types.AllianceFactSheetInformation();
            allianceInfos.Deserialize(reader);
            var membersCount = reader.ReadUShort();
            var members_ = new Types.AllianceMemberInfo[membersCount];
            for (var membersIndex = 0; membersIndex < membersCount; membersIndex++)
            {
                 var objectToAdd = new Types.AllianceMemberInfo();
                 objectToAdd.Deserialize(reader);
                 members_[membersIndex] = objectToAdd;
            }
            members = members_;
            var prismsCount = reader.ReadUShort();
            var prisms_ = new Types.PrismGeolocalizedInformation[prismsCount];
            for (var prismsIndex = 0; prismsIndex < prismsCount; prismsIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.PrismGeolocalizedInformation>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 prisms_[prismsIndex] = objectToAdd;
            }
            prisms = prisms_;
            var taxCollectorsCount = reader.ReadUShort();
            var taxCollectors_ = new Types.TaxCollectorInformations[taxCollectorsCount];
            for (var taxCollectorsIndex = 0; taxCollectorsIndex < taxCollectorsCount; taxCollectorsIndex++)
            {
                 var objectToAdd = new Types.TaxCollectorInformations();
                 objectToAdd.Deserialize(reader);
                 taxCollectors_[taxCollectorsIndex] = objectToAdd;
            }
            taxCollectors = taxCollectors_;
        }
    }
}
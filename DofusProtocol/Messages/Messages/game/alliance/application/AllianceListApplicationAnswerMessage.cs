

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class AllianceListApplicationAnswerMessage : PaginationAnswerAbstractMessage
    {
        public const uint Id = 122;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.SocialApplicationInformation> applies;
        
        public AllianceListApplicationAnswerMessage()
        { }
        
        public AllianceListApplicationAnswerMessage(double offset, uint count, uint total, IEnumerable<Types.SocialApplicationInformation> applies)
         : base(offset, count, total)
        {
            this.applies = applies;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteShort((short)applies.Count());
            foreach (var objectToSend in applies)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            var appliesCount = reader.ReadUShort();
            var applies_ = new Types.SocialApplicationInformation[appliesCount];
            for (var appliesIndex = 0; appliesIndex < appliesCount; appliesIndex++)
            {
                 var objectToAdd = new Types.SocialApplicationInformation();
                 objectToAdd.Deserialize(reader);
                 applies_[appliesIndex] = objectToAdd;
            }
            applies = applies_;
        }
    }
}
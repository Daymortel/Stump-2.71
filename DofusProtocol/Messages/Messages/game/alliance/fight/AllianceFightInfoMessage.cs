

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class AllianceFightInfoMessage : Message
    {
        public const uint Id = 465;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.SocialFight> allianceFights;
        
        public AllianceFightInfoMessage()
        { }
        
        public AllianceFightInfoMessage(IEnumerable<Types.SocialFight> allianceFights)
        {
            this.allianceFights = allianceFights;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)allianceFights.Count());
            foreach (var objectToSend in allianceFights)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var allianceFightsCount = reader.ReadUShort();
            var allianceFights_ = new Types.SocialFight[allianceFightsCount];
            for (var allianceFightsIndex = 0; allianceFightsIndex < allianceFightsCount; allianceFightsIndex++)
            {
                 var objectToAdd = new Types.SocialFight();
                 objectToAdd.Deserialize(reader);
                 allianceFights_[allianceFightsIndex] = objectToAdd;
            }
            allianceFights = allianceFights_;
        }
    }
}
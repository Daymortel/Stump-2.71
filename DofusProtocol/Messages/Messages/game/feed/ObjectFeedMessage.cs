

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ObjectFeedMessage : Message
    {
        public const uint Id = 6700;
        public override uint MessageId => Id;
        
        public uint objectUID;
        public IEnumerable<Types.ObjectItemQuantity> meal;
        
        public ObjectFeedMessage()
        { }
        
        public ObjectFeedMessage(uint objectUID, IEnumerable<Types.ObjectItemQuantity> meal)
        {
            this.objectUID = objectUID;
            this.meal = meal;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteVarUInt(objectUID);
            writer.WriteShort((short)meal.Count());
            foreach (var objectToSend in meal)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            objectUID = reader.ReadVarUInt();
            var mealCount = reader.ReadUShort();
            var meal_ = new Types.ObjectItemQuantity[mealCount];
            for (var mealIndex = 0; mealIndex < mealCount; mealIndex++)
            {
                 var objectToAdd = new Types.ObjectItemQuantity();
                 objectToAdd.Deserialize(reader);
                 meal_[mealIndex] = objectToAdd;
            }
            meal = meal_;
        }
    }
}
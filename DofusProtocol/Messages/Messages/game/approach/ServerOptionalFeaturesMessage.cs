

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ServerOptionalFeaturesMessage : Message
    {
        public const uint Id = 6599;
        public override uint MessageId => Id;
        
        public IEnumerable<int> features;
        
        public ServerOptionalFeaturesMessage()
        { }
        
        public ServerOptionalFeaturesMessage(IEnumerable<int> features)
        {
            this.features = features;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)features.Count());
            foreach (var objectToSend in features)
            {
                 writer.WriteInt(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var featuresCount = reader.ReadUShort();
            var features_ = new int[featuresCount];
            for (var featuresIndex = 0; featuresIndex < featuresCount; featuresIndex++)
            {
                 features_[featuresIndex] = reader.ReadInt();
            }
            features = features_;
        }
    }
}
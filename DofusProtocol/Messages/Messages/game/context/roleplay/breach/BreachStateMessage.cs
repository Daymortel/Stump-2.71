

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class BreachStateMessage : Message
    {
        public const uint Id = 4452;
        public override uint MessageId => Id;
        
        public Types.CharacterMinimalInformations owner;
        public IEnumerable<Types.ObjectEffectInteger> bonuses;
        public uint bugdet;
        public bool saved;
        
        public BreachStateMessage()
        { }
        
        public BreachStateMessage(Types.CharacterMinimalInformations owner, IEnumerable<Types.ObjectEffectInteger> bonuses, uint bugdet, bool saved)
        {
            this.owner = owner;
            this.bonuses = bonuses;
            this.bugdet = bugdet;
            this.saved = saved;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            owner.Serialize(writer);
            writer.WriteShort((short)bonuses.Count());
            foreach (var objectToSend in bonuses)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteVarUInt(bugdet);
            writer.WriteBoolean(saved);
        }
        
        public override void Deserialize(IDataReader reader)
        {
            owner = new Types.CharacterMinimalInformations();
            owner.Deserialize(reader);
            var bonusesCount = reader.ReadUShort();
            var bonuses_ = new Types.ObjectEffectInteger[bonusesCount];
            for (var bonusesIndex = 0; bonusesIndex < bonusesCount; bonusesIndex++)
            {
                 var objectToAdd = new Types.ObjectEffectInteger();
                 objectToAdd.Deserialize(reader);
                 bonuses_[bonusesIndex] = objectToAdd;
            }
            bonuses = bonuses_;
            bugdet = reader.ReadVarUInt();
            saved = reader.ReadBoolean();
        }
    }
}
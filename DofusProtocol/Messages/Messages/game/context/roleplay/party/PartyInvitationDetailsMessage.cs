

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class PartyInvitationDetailsMessage : AbstractPartyMessage
    {
        public const uint Id = 634;
        public override uint MessageId => Id;
        
        public sbyte partyType;
        public string partyName;
        public ulong fromId;
        public string fromName;
        public ulong leaderId;
        public IEnumerable<Types.PartyInvitationMemberInformations> members;
        public IEnumerable<Types.PartyGuestInformations> guests;
        
        public PartyInvitationDetailsMessage()
        { }
        
        public PartyInvitationDetailsMessage(uint partyId, sbyte partyType, string partyName, ulong fromId, string fromName, ulong leaderId, IEnumerable<Types.PartyInvitationMemberInformations> members, IEnumerable<Types.PartyGuestInformations> guests)
         : base(partyId)
        {
            this.partyType = partyType;
            this.partyName = partyName;
            this.fromId = fromId;
            this.fromName = fromName;
            this.leaderId = leaderId;
            this.members = members;
            this.guests = guests;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            base.Serialize(writer);
            writer.WriteSByte(partyType);
            writer.WriteUTF(partyName);
            writer.WriteVarULong(fromId);
            writer.WriteUTF(fromName);
            writer.WriteVarULong(leaderId);
            writer.WriteShort((short)members.Count());
            foreach (var objectToSend in members)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
            writer.WriteShort((short)guests.Count());
            foreach (var objectToSend in guests)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            base.Deserialize(reader);
            partyType = reader.ReadSByte();
            partyName = reader.ReadUTF();
            fromId = reader.ReadVarULong();
            fromName = reader.ReadUTF();
            leaderId = reader.ReadVarULong();
            var membersCount = reader.ReadUShort();
            var members_ = new Types.PartyInvitationMemberInformations[membersCount];
            for (var membersIndex = 0; membersIndex < membersCount; membersIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.PartyInvitationMemberInformations>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 members_[membersIndex] = objectToAdd;
            }
            members = members_;
            var guestsCount = reader.ReadUShort();
            var guests_ = new Types.PartyGuestInformations[guestsCount];
            for (var guestsIndex = 0; guestsIndex < guestsCount; guestsIndex++)
            {
                 var objectToAdd = new Types.PartyGuestInformations();
                 objectToAdd.Deserialize(reader);
                 guests_[guestsIndex] = objectToAdd;
            }
            guests = guests_;
        }
    }
}
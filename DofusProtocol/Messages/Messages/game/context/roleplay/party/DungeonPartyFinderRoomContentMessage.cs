

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class DungeonPartyFinderRoomContentMessage : Message
    {
        public const uint Id = 1307;
        public override uint MessageId => Id;
        
        public ushort dungeonId;
        public IEnumerable<Types.DungeonPartyFinderPlayer> players;
        
        public DungeonPartyFinderRoomContentMessage()
        { }
        
        public DungeonPartyFinderRoomContentMessage(ushort dungeonId, IEnumerable<Types.DungeonPartyFinderPlayer> players)
        {
            this.dungeonId = dungeonId;
            this.players = players;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteVarUShort(dungeonId);
            writer.WriteShort((short)players.Count());
            foreach (var objectToSend in players)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            dungeonId = reader.ReadVarUShort();
            var playersCount = reader.ReadUShort();
            var players_ = new Types.DungeonPartyFinderPlayer[playersCount];
            for (var playersIndex = 0; playersIndex < playersCount; playersIndex++)
            {
                 var objectToAdd = new Types.DungeonPartyFinderPlayer();
                 objectToAdd.Deserialize(reader);
                 players_[playersIndex] = objectToAdd;
            }
            players = players_;
        }
    }
}
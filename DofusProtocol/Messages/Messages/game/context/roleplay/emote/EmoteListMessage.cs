

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class EmoteListMessage : Message
    {
        public const uint Id = 8395;
        public override uint MessageId => Id;
        
        public IEnumerable<ushort> emoteIds;
        
        public EmoteListMessage()
        { }
        
        public EmoteListMessage(IEnumerable<ushort> emoteIds)
        {
            this.emoteIds = emoteIds;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)emoteIds.Count());
            foreach (var objectToSend in emoteIds)
            {
                 writer.WriteUShort(objectToSend);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var emoteIdsCount = reader.ReadUShort();
            var emoteIds_ = new ushort[emoteIdsCount];
            for (var emoteIdsIndex = 0; emoteIdsIndex < emoteIdsCount; emoteIdsIndex++)
            {
                 emoteIds_[emoteIdsIndex] = reader.ReadUShort();
            }
            emoteIds = emoteIds_;
        }
    }
}
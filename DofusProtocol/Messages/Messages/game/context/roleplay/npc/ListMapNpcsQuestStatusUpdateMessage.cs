

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ListMapNpcsQuestStatusUpdateMessage : Message
    {
        public const uint Id = 233;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.MapNpcQuestInfo> mapInfo;
        
        public ListMapNpcsQuestStatusUpdateMessage()
        { }
        
        public ListMapNpcsQuestStatusUpdateMessage(IEnumerable<Types.MapNpcQuestInfo> mapInfo)
        {
            this.mapInfo = mapInfo;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)mapInfo.Count());
            foreach (var objectToSend in mapInfo)
            {
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var mapInfoCount = reader.ReadUShort();
            var mapInfo_ = new Types.MapNpcQuestInfo[mapInfoCount];
            for (var mapInfoIndex = 0; mapInfoIndex < mapInfoCount; mapInfoIndex++)
            {
                 var objectToAdd = new Types.MapNpcQuestInfo();
                 objectToAdd.Deserialize(reader);
                 mapInfo_[mapInfoIndex] = objectToAdd;
            }
            mapInfo = mapInfo_;
        }
    }
}
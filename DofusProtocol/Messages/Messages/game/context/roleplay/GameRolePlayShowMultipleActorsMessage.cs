

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameRolePlayShowMultipleActorsMessage : Message
    {
        public const uint Id = 6824;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GameRolePlayActorInformations> informationsList;
        
        public GameRolePlayShowMultipleActorsMessage()
        { }
        
        public GameRolePlayShowMultipleActorsMessage(IEnumerable<Types.GameRolePlayActorInformations> informationsList)
        {
            this.informationsList = informationsList;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)informationsList.Count());
            foreach (var objectToSend in informationsList)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var informationsListCount = reader.ReadUShort();
            var informationsList_ = new Types.GameRolePlayActorInformations[informationsListCount];
            for (var informationsListIndex = 0; informationsListIndex < informationsListCount; informationsListIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.GameRolePlayActorInformations>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 informationsList_[informationsListIndex] = objectToAdd;
            }
            informationsList = informationsList_;
        }
    }
}
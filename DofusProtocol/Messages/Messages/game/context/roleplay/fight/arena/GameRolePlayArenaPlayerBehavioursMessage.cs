

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameRolePlayArenaPlayerBehavioursMessage : Message
    {
        public const uint Id = 6735;
        public override uint MessageId => Id;
        
        public IEnumerable<string> flags;
        public IEnumerable<string> sanctions;
        public int banDuration;
        
        public GameRolePlayArenaPlayerBehavioursMessage()
        { }
        
        public GameRolePlayArenaPlayerBehavioursMessage(IEnumerable<string> flags, IEnumerable<string> sanctions, int banDuration)
        {
            this.flags = flags;
            this.sanctions = sanctions;
            this.banDuration = banDuration;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)flags.Count());
            foreach (var objectToSend in flags)
            {
                 writer.WriteUTF(objectToSend);
            }
            writer.WriteShort((short)sanctions.Count());
            foreach (var objectToSend in sanctions)
            {
                 writer.WriteUTF(objectToSend);
            }
            writer.WriteInt(banDuration);
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var flagsCount = reader.ReadUShort();
            var flags_ = new string[flagsCount];
            for (var flagsIndex = 0; flagsIndex < flagsCount; flagsIndex++)
            {
                 flags_[flagsIndex] = reader.ReadUTF();
            }
            flags = flags_;
            var sanctionsCount = reader.ReadUShort();
            var sanctions_ = new string[sanctionsCount];
            for (var sanctionsIndex = 0; sanctionsIndex < sanctionsCount; sanctionsIndex++)
            {
                 sanctions_[sanctionsIndex] = reader.ReadUTF();
            }
            sanctions = sanctions_;
            banDuration = reader.ReadInt();
        }
    }
}


//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameRolePlayArenaSwitchToFightServerMessage : Message
    {
        public const uint Id = 7992;
        public override uint MessageId => Id;
        
        public string address;
        public IEnumerable<ushort> ports;
        public string token;
        
        public GameRolePlayArenaSwitchToFightServerMessage()
        { }
        
        public GameRolePlayArenaSwitchToFightServerMessage(string address, IEnumerable<ushort> ports, string token)
        {
            this.address = address;
            this.ports = ports;
            this.token = token;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteUTF(address);
            writer.WriteShort((short)ports.Count());
            foreach (var objectToSend in ports)
            {
                 writer.WriteVarUShort(objectToSend);
            }
            writer.WriteUTF(token);
        }
        
        public override void Deserialize(IDataReader reader)
        {
            address = reader.ReadUTF();
            var portsCount = reader.ReadUShort();
            var ports_ = new ushort[portsCount];
            for (var portsIndex = 0; portsIndex < portsCount; portsIndex++)
            {
                 ports_[portsIndex] = reader.ReadVarUShort();
            }
            ports = ports_;
            token = reader.ReadUTF();
        }
    }
}
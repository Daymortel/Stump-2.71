

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class ChallengeProposalMessage : Message
    {
        public const uint Id = 5972;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.ChallengeInformation> challengeProposals;
        public double timer;
        
        public ChallengeProposalMessage()
        { }
        
        public ChallengeProposalMessage(IEnumerable<Types.ChallengeInformation> challengeProposals, double timer)
        {
            this.challengeProposals = challengeProposals;
            this.timer = timer;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)challengeProposals.Count());
            foreach (var objectToSend in challengeProposals)
            {
                 objectToSend.Serialize(writer);
            }
            writer.WriteDouble(timer);
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var challengeProposalsCount = reader.ReadUShort();
            var challengeProposals_ = new Types.ChallengeInformation[challengeProposalsCount];
            for (var challengeProposalsIndex = 0; challengeProposalsIndex < challengeProposalsCount; challengeProposalsIndex++)
            {
                 var objectToAdd = new Types.ChallengeInformation();
                 objectToAdd.Deserialize(reader);
                 challengeProposals_[challengeProposalsIndex] = objectToAdd;
            }
            challengeProposals = challengeProposals_;
            timer = reader.ReadDouble();
        }
    }
}
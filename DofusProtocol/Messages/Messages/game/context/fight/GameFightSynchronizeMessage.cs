

//Generated by Kenshin
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Stump.Core.IO;
using Stump.DofusProtocol.Types;

namespace Stump.DofusProtocol.Messages
{
    public class GameFightSynchronizeMessage : Message
    {
        public const uint Id = 4424;
        public override uint MessageId => Id;
        
        public IEnumerable<Types.GameFightFighterInformations> fighters;
        
        public GameFightSynchronizeMessage()
        { }
        
        public GameFightSynchronizeMessage(IEnumerable<Types.GameFightFighterInformations> fighters)
        {
            this.fighters = fighters;
        }
        
        public override void Serialize(IDataWriter writer)
        {
            writer.WriteShort((short)fighters.Count());
            foreach (var objectToSend in fighters)
            {
                 writer.WriteShort(objectToSend.TypeId);
                 objectToSend.Serialize(writer);
            }
        }
        
        public override void Deserialize(IDataReader reader)
        {
            var fightersCount = reader.ReadUShort();
            var fighters_ = new Types.GameFightFighterInformations[fightersCount];
            for (var fightersIndex = 0; fightersIndex < fightersCount; fightersIndex++)
            {
                 var objectToAdd = Types.ProtocolTypeManager.GetInstance<Types.GameFightFighterInformations>(reader.ReadShort());
                 objectToAdd.Deserialize(reader);
                 fighters_[fightersIndex] = objectToAdd;
            }
            fighters = fighters_;
        }
    }
}